# Copy to .env.local for local overrides, or set in the build environment.
# In dev these are unset and the Vite proxy forwards /ws-stomp to localhost:8080.
# In production (Firebase), point this at the Cloud Run backend (use https URL for SockJS):
# VITE_WS_URL=https://denden-homework-813602138003.asia-southeast1.run.app/ws-stomp
VITE_WS_URL=
